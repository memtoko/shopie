import ctrlOrCmd from '../../../utils/ctrl-or-cmd';
import { module, test } from 'qunit';

module('Unit | Utility | ctrl or cmd');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ctrlOrCmd();
  assert.ok(result);
});
