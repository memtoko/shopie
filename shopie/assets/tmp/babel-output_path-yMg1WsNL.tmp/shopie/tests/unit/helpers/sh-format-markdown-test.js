import { shFormatMarkdown } from '../../../helpers/sh-format-markdown';
import { module, test } from 'qunit';

module('Unit | Helper | sh format markdown');

// Replace this with your real tests.
test('it works', function (assert) {
  var result = shFormatMarkdown(42);
  assert.ok(result);
});