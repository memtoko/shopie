import { cycleIt } from '../../../helpers/cycle-it';
import { module, test } from 'qunit';

module('Unit | Helper | cycle it');

// Replace this with your real tests.
test('it works', function (assert) {
  var result = cycleIt(42);
  assert.ok(result);
});