import shopiePaths from '../../../utils/shopie-paths';
import { module, test } from 'qunit';

module('Unit | Utility | shopie paths');

// Replace this with your real tests.
test('it works', function (assert) {
  var result = shopiePaths();
  assert.ok(result);
});