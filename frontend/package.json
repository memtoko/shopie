{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp browserify --optimise --to dist/shopie.js",
    "watch": "pulp -w browserify --to dist/shopie.js",
    "build:css": "myth styles/app.css dist/shopie.css --sourcemap=true --compress=true"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "myth": "^1.5.0",
    "purescript-psa": "^0.3.9"
  }
}
